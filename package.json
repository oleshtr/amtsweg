{
  "name": "amtsweg",
  "version": "0.1.0",
  "private": true,
  "description": "AMTSWEG – ein kleines browserbasiertes Pixel-Art Idle-Game über eine fiktionale politische Karriere in Deutschland.",
  "scripts": {
    "start": "node server.js",
    "check": "node --check src/game.js && node --check src/storage.js && node --check src/world.js && node --check src/main.js && node --check server.js",
    "test": "node --test tests/*.test.js",
    "test:browser": "node tests/browser.cjs"
  },
  "engines": {
    "node": ">=18"
  }
}
